const inputArr = [];

//const wrapper = //find element

for (let i = 0; i < inputArr.length; i++) {

//builder(inputArr[i],wrapper);

}


function builder(arr, component) {

    let tempComponent = component;

    let tempArr = arr;

    //create button with tempArr[0]
    //assign classes to button
    //add buttn label as well
    //add eventlisteners

    //tempComponent.append(button created now)

    //create new info component

    // If (tempArr[1] == "E") {
        //info component add expand classes

//    } else {
       // info component assign Sliding classes 
//}
    // if tempArr[3] == 0 {
       // return
    //} else{
        //return builder(temArr,tempComponent)
    //}

}